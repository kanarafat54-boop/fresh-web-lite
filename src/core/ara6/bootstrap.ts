/**
 * Fresh Web Lite
 * Ara6 bootstrap — registers tools, capabilities, operational agents and
 * the search evolution graph so runtime state reflects real capabilities.
 */

import "./defaultTools";
import "./defaultCapabilities";
import "./agents/defaultAgents";
import "./evolution/registerEvolution";
