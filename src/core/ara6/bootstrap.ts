/**
 * Fresh Web Lite
 * Ara6 bootstrap — registers the default tools, capabilities, and the
 * search evolution graph so the runtime reflects real state instead of
 * hardcoded placeholders. Imported once wherever the Ara6 runtime boots.
 */

import "./defaultTools";
import "./defaultCapabilities";
import "./evolution/registerEvolution";
