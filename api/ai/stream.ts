import { createClient } from "@supabase/supabase-js";
import { createFreshAIIntelligenceGateway } from "../../src/core/fresh-ai/FreshAIIntelligenceGateway.js";
import { createCoreFreshSkillRegistry } from "../../src/core/fresh-ai/FreshAISkillFabric.js";
import type { Evidence, FreshConversationTurn } from "../../src/core/fresh-ai/FreshAIArchitecture.js";
import type { FreshMemoryRecord } from "../../src/core/fresh-ai/FreshAIKernel.js";
import { createFreshAIWorkspaceContext, type FreshAIWorkspaceContext } from "../../src/core/fresh-ai/FreshAIWorkspaceContext.js";
import "../../src/core/ara6/agents/defaultAgents.js";
export const config = { maxDuration: 60 };
type Body = { goal?: string; route?: string; approve?: boolean; conversationId?: string; conversation?: FreshConversationTurn[]; workspaceContext?: FreshAIWorkspaceContext; model?: string; voiceModel?: string };
type MemoryRow = { id: string; content: string; scope: FreshMemoryRecord["scope"]; created_at: string; source: string };
const encoder = new TextEncoder();
const event = (type: string, payload: unknown) => encoder.encode(`data: ${JSON.stringify({ type, ...((payload && typeof payload === "object") ? payload : { value: payload })})}\n\n`);
const service = () => { const u = process.env.SUPABASE_URL || process.env.VITE_SUPABASE_URL, k = process.env.SUPABASE_SERVICE_ROLE_KEY; return u && k ? createClient(u, k, { auth: { persistSession: false, autoRefreshToken: false } }) : null; };
async function auth(req: Request) { const u = process.env.VITE_SUPABASE_URL, k = process.env.VITE_SUPABASE_ANON_KEY, a = req.headers.get("authorization") || "", t = a.startsWith("Bearer ") ? a.slice(7) : ""; if (!u || !k || !t) return null; const c = createClient(u, k, { auth: { persistSession: false, autoRefreshToken: false } }); const r = await c.auth.getUser(t); return r.data.user ?? null; }
function memory(userId: string | null) { const c = service(); return { search: async (q: string, scope?: FreshMemoryRecord["scope"]) => { if (!c || !userId) return [] as FreshMemoryRecord[]; let query = c.from("fresh_ai_memory").select("id,content,scope,created_at,source").eq("user_id", userId).order("updated_at", { ascending: false }).limit(24); if (scope) query = query.eq("scope", scope); const r = await query; const words = q.toLowerCase().split(/\s+/).filter(x => x.length > 3); return (r.data ?? []).filter((x: MemoryRow) => !words.length || words.some(w => x.content.toLowerCase().includes(w))).slice(0, 8).map((x: MemoryRow) => ({ id: x.id, content: x.content, scope: x.scope, createdAt: x.created_at, source: x.source })) as FreshMemoryRecord[]; }, remember: async (x: FreshMemoryRecord) => { if (c && userId) await c.from("fresh_ai_memory").upsert({ id: x.id, user_id: userId, scope: x.scope, content: x.content, source: x.source, created_at: x.createdAt, updated_at: new Date().toISOString() }); } }; }
async function research(goal: string): Promise<Evidence[]> { const key = process.env.TAVILY_API_KEY; if (!key) return []; try { const r = await fetch("https://api.tavily.com/search", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ api_key: key, query: goal, search_depth: "advanced", topic: "general", max_results: 8, include_answer: false }) }); if (!r.ok) return []; const p = await r.json() as { results?: Array<{ title?: string; url?: string; content?: string; published_date?: string }> }; return (p.results ?? []).filter(x => x.title && x.url).map((x, i) => ({ id: `web-${i + 1}`, source: x.url!, claim: `${x.title}: ${x.content || x.title}`, observedAt: x.published_date, confidence: .7 })); } catch { return []; } }
async function conversationLedger(userId: string | null, conversationId: string | undefined, route: string, workspace: FreshAIWorkspaceContext, model: string | undefined, goal: string, requestId: string, evidence: Evidence[], answer: string) { const c = service(); if (!c || !userId) return conversationId ?? null; let id = conversationId; if (id) { const check = await c.from("fresh_ai_conversations").select("id").eq("id", id).eq("user_id", userId).maybeSingle(); if (!check.data) id = undefined; } if (!id) { const created = await c.from("fresh_ai_conversations").insert({ user_id: userId, title: goal.slice(0, 100), route, surface: workspace.surface, model_id: model ?? "fresh-auto" }).select("id").single(); if (created.error || !created.data) return null; id = created.data.id; } const rows = [{ conversation_id: id, user_id: userId, request_id: requestId, role: "user", content: goal, model_id: model ?? "fresh-auto" }, ...(answer ? [{ conversation_id: id, user_id: userId, request_id: requestId, role: "assistant", content: answer, model_id: model ?? "fresh-auto", evidence: evidence.map(x => ({ title: x.source, snippet: x.claim })) }] : [])]; const inserted = await c.from("fresh_ai_conversation_turns").insert(rows); if (inserted.error) return null; await c.from("fresh_ai_conversations").update({ route, surface: workspace.surface, model_id: model ?? "fresh-auto", updated_at: new Date().toISOString() }).eq("id", id).eq("user_id", userId); return id; }
export async function POST(req: Request): Promise<Response> { const requestId = crypto.randomUUID(); const user = await auth(req); const controller = new AbortController(); req.signal.addEventListener("abort", () => controller.abort(), { once: true }); try { const b = await req.json() as Body, goal = typeof b.goal === "string" ? b.goal.trim() : "", route = typeof b.route === "string" ? b.route.trim().slice(0, 120) : "/ai", conversation = Array.isArray(b.conversation) ? b.conversation.filter(x => x && typeof x.content === "string" && (x.role === "user" || x.role === "assistant")).slice(-8) : [], workspace = b.workspaceContext && typeof b.workspaceContext === "object" ? b.workspaceContext : createFreshAIWorkspaceContext(route); if (!goal) return new Response(JSON.stringify({ error: "A goal is required" }), { status: 400, headers: { "content-type": "application/json" } }); if (goal.length > 4000) return new Response(JSON.stringify({ error: "Keep the request under 4,000 characters" }), { status: 400, headers: { "content-type": "application/json" } }); const uid = user?.id ?? null; const gateway = createFreshAIIntelligenceGateway({ skills: createCoreFreshSkillRegistry(), memory: memory(uid), retrieve: async (x, i) => i.needsEvidence || i.intent === "research" ? research(x) : [] }); const result = await gateway.handle({ requestId, input: goal, route, userId: uid, conversation, context: { workspace, model: b.model ?? "fresh-auto", voiceModel: b.voiceModel ?? null }, approve: Boolean(b.approve), execute: true }); const evidence = (result.result.claims ?? []).flatMap(x => Array.isArray(x.evidence) ? x.evidence : []) as Evidence[]; const verification = { evidenceCount: evidence.length, confidence: evidence.length >= 3 ? "medium" : "low", contradictionsDetected: (result.result.claims ?? []).some(x => x.truth === "CONTRADICTED") }; const stream = new ReadableStream<Uint8Array>({ async start(out) { const answer = result.result.answer || "Fresh AI completed the request without a text answer."; out.enqueue(event("start", { requestId, conversationId: b.conversationId ?? null, status: result.status, intent: result.interpretation.intent, workspace, universal: result.universal, verification })); out.enqueue(event("token", { text: answer })); const persisted = await conversationLedger(uid, b.conversationId, route, workspace, b.model, goal, requestId, evidence, answer); out.enqueue(event("done", { requestId, conversationId: persisted ?? b.conversationId ?? null, model: b.model ?? "fresh-auto", verification, evidence })); out.close(); }, cancel() { controller.abort(); } }); return new Response(stream, { headers: { "content-type": "text/event-stream; charset=utf-8", "cache-control": "no-cache, no-transform", connection: "keep-alive", "x-accel-buffering": "no" } }); } catch (error) { return new Response(JSON.stringify({ requestId, error: error instanceof Error ? error.message : "Fresh AI stream failed" }), { status: 500, headers: { "content-type": "application/json", "cache-control": "no-store" } }); } }
