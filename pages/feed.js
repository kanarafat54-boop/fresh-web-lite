import Layout from '../components/Layout'

export default function Feed() {
  return (
    <Layout title="Feed — fresh-web-lite">
      <h1>Feed</h1>
      <p>This is a placeholder feed page. Wire this to your API or Prisma queries (lib/prisma) to render real posts.</p>
    </Layout>
  )
}
